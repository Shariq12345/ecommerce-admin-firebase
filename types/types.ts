export interface Store {
  id: string;
  name: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}

export interface Billboards {
  id: string;
  label: string;
  imageUrl: string;
  createdAt: string;
  updatedAt: string;
}
